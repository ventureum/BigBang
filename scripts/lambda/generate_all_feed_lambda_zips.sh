#!/usr/bin/env bash

#echo "generating attach_session zip"
#.//generate_lambda_zip.sh feed attach_session $1 $2
#
#echo "generating deactivate_actor zip"
#.//generate_lambda_zip.sh feed deactivate_actor $1 $2
#
#echo "generating dev_refuel zip"
#.//generate_lambda_zip.sh feed dev_refuel $1 $2
#
#echo "generating feed_events_table_creator zip"
#.//generate_lambda_zip.sh feed feed_events_table_creator $1 $2
#
#echo "generating feed_post zip"
#.//generate_lambda_zip.sh feed feed_post $1 $2
#
#echo "generating feed_token_generator zip"
#.//generate_lambda_zip.sh feed feed_token_generator $1 $2
#
#echo "generating feed_update_post_rewards zip"
#.//generate_lambda_zip.sh feed feed_update_post_rewards $1 $2
#
#echo "generating feed_upvote zip"
#.//generate_lambda_zip.sh feed feed_upvote $1 $2
#

#echo "generating get_actor_private_key zip"
#.//generate_lambda_zip.sh feed get_actor_private_key $1 $2

#echo "generating get_actor_uuid_from_public_key zip"
#.//generate_lambda_zip.sh feed get_actor_uuid_from_public_key $1 $2

#echo "generating get_batch_posts zip"
#.//generate_lambda_zip.sh feed get_batch_posts $1 $2
#
#echo "generating get_feed_post zip"
#.//generate_lambda_zip.sh feed get_feed_post $1 $2
#
#echo "generating get_profile zip"
#.//generate_lambda_zip.sh feed get_profile $1 $2
#
#echo "generating get_recent_posts zip"
#.//generate_lambda_zip.sh feed get_recent_posts $1 $2
#
#echo "generating get_recent_votes zip"
#.//generate_lambda_zip.sh feed get_recent_votes $1 $2
#
#echo "generating get_session zip"
#.//generate_lambda_zip.sh feed get_session $1 $2
#
#echo "generating profile zip"
#.//generate_lambda_zip.sh feed profile $1 $2
#
#echo "generating refuel zip"
#.//generate_lambda_zip.sh feed refuel $1 $2
#

#echo "generating set_actor_private_key zip"
#.//generate_lambda_zip.sh feed set_actor_private_key $1 $2

#echo "generating reset_actor_refuel zip"
#.//generate_lambda_zip.sh feed reset_actor_fuel $1 $2

#echo "generating add_tracked_wallet_addresses zip"
#.//generate_lambda_zip.sh feed add_tracked_wallet_addresses $1 $2
#
#echo "generating delete_tracked_wallet_addresses zip"
#.//generate_lambda_zip.sh feed delete_tracked_wallet_addresses $1 $2

#echo "generating get_tracked_wallet_addresses zip"
#.//generate_lambda_zip.sh feed get_tracked_wallet_addresses $1 $2

#echo "generating get_batch_profiles zip"
#.//generate_lambda_zip.sh feed get_batch_profiles $1 $2

#echo "generating set_token_pool zip"
#.//generate_lambda_zip.sh feed set_token_pool  $1 $2
#
#echo "generating set_next_redeem zip"
#.//generate_lambda_zip.sh feed set_next_redeem  $1 $2
#
#echo "generating get_next_redeem zip"
#.//generate_lambda_zip.sh feed get_next_redeem  $1 $2
#
#echo "generating get_redeem_history zip"
#.//generate_lambda_zip.sh feed get_redeem_history  $1 $2
#
#echo "generating get_redeem_block_info zip"
#.//generate_lambda_zip.sh feed get_redeem_block_info  $1 $2

echo "generating feed_redeem_milestone_points zip"
.//generate_lambda_zip.sh feed feed_redeem_milestone_points $1 $2
